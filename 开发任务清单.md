> ⚠️ **【部分条目已过期 — 2026-06-11】**
> 四、工单流程引擎重构（编号8-17）大部分已完成，状态未更新。**流程权威规范请参见 `.trae/skills/sa-workorder-flow/SKILL.md`**。
>
> 以下为原始任务清单：

# 山善SA系统 — 开发任务清单

> 状态说明：🔴待开发  🟡开发中  🟢已完成

---

## 一、角色名称替换

| 编号 | 状态 | 任务 | 涉及文件 |
|------|------|------|---------|
| 1 | 🟢 | `业务员` → `业务助理`（全局中文替换，key `sales` 不变） | 18个文件 |
| 2 | 🟢 | `技术部长` + `技术主管` → `课长`（全局中文替换，key `techLead` 不变） | 14个文件 |

> **✅ 编号1 修改逻辑**：全局搜索 `业务员` 共 51 处，全部替换为 `业务助理`。role key `sales` 不变。模拟人名同步更新：张业务员→张业务助理、李业务员→李业务助理、王业务员→王业务助理、业务员A→赵业务助理、业务员B→钱业务助理。涉及所有含销售角色相关的 Vue 组件和 Store。

> **✅ 编号2 修改逻辑**：全局搜索 `技术部长` 和 `技术主管` 共 82 处，全部替换为 `课长`。

## 新增：部长角色

| 编号 | 状态 | 任务 | 说明 |
|------|------|------|------|
| 1-附 | 🟢 | 新增部长角色（key `director`），当前功能等同于课长 | router + 登录页 + App + 权限判断 |

> **✅ 修改逻辑**：role key 为 `director`，中文名"部长"。当前路由权限、首页跳转、设备切换按钮、审批权限、通知可见范围均等同于课长。部长登录后可查看课长的通知。后续流程改造时部长再独立分化为上级审批节点。涉及文件：router/index.js、App.vue（roleNames + deviceSwitch）、UnifiedLogin.vue（角色卡片）、StaffLogin.vue（快速登录 + 账号映射）、StaffMobileWorkspace.vue（isApprover/isManager/hasTeamAccess）、StaffWorkorderList.vue（isManager）、StaffFieldCheckinList.vue（isApprover）、StaffProfile.vue、StaffProfileDesktop.vue、Workorder.vue（报价审核权限）、notificationStore.js（部长共享课长通知）。

## 二、课长菜单精简

| 编号 | 状态 | 任务 |
|------|------|------|
| 3 | � | 课长 PC 端隐藏「团队管理」「团队统计」「成员详情」菜单/路由入口 |

> **✅ 修改逻辑**：① `router/index.js` 的 `rolePermissions.techLead` 数组移除 `/team-management` `/team-statistics` `/member-detail` 3条路由权限。② `StaffMobileWorkspace.vue` 增加 `hasTeamAccess` 计算属性（排除课长），团队概览统计卡片、团队成员列表、快捷功能中的团队管理和团队统计入口均改为 `v-if="hasTeamAccess"`，课长不再展示。审批管理和工单按钮正常保留。

## 三、手机端打卡改造

| 编号 | 状态 | 任务 |
|------|------|------|
| 4 | � | 多次签离：每次覆盖上次签离时间，仅保留最后一次。手动"提交审批"后锁定 |
| 5 | � | 手机端审批对话框：展示完整打卡详情（类型/工单/时间/定位/照片/工时），课长审批通过 |
| 6 | � | 签到签离定位：新建 `src/utils/gps.js`，使用浏览器原生 `navigator.geolocation` API 获取真实 GPS 坐标，失败降级为模拟数据 |
| 7 | 🔴 | 日报打卡新增字段（待确认具体字段） |

> **✅ 编号4 修改逻辑**：`StaffFieldCheckinList.vue` 中 `submitCheckOut` 仅更新 `checkoutTime` 和 `checkOutLocation`，不改变 `status`（保持"进行中"），签离按钮始终可见。用户可多次签离覆盖上次时间。`handleSubmitApproval` 锁定状态为"已提交"。工单打卡和活动打卡逻辑一致。
>
> **✅ 编号5 修改逻辑**：新增审批对话框（含基本信息/打卡时间/位置信息/工时统计/现场照片/审批意见6个区块），`handleApprove` 改为打开对话框而非直接通过，`confirmApprove` 执行审批。审批结果：仅"通过"，不设驳回。
>
> **✅ 编号6 修改逻辑**：新建 `src/utils/gps.js`，封装 `getCurrentPosition()` 返回 `{latitude, longitude, address, timestamp}`。`StaffCheckIn.vue` 签到定位和 `StaffFieldCheckinList.vue` 签离定位均调用此工具，不支持 GPS 时自动降级为模拟数据。

## 四、工单流程引擎重构（核心）

| 编号 | 状态 | 任务 | 说明 |
|------|------|------|------|
| 8 | 🔴 | 定义新流程节点 | 创建 → 待分配 → 待接单 → 进行中 → 待签字 → 课长确认 → 业务确认 → 工单完成 |
| 9 | 🔴 | 创建节点 | 业务助理/客户创建，按工单类型推送不同课长 |
| 10 | 🔴 | 待分配 + 超时报警 | 课长分配工程师；2h 未分配报警给管理员 |
| 11 | 🔴 | 待接单 + 超时报警 | 工程师接单/弃单（流转回课长）；2h 未接单报警给课长+管理员 |
| 12 | 🔴 | 进行中 | 日报打卡、临时保存报告（关闭后可恢复）、点击"客户签字"→待签字 |
| 13 | 🔴 | 待签字 | 任一方签字→流转；签字后自动生成带签名 PDF 留存工单 |
| 14 | 🔴 | 课长确认 | 查看并点击确认 |
| 15 | 🔴 | 业务确认 | 业务助理查看并点击确认 |
| 16 | 🔴 | 全节点时间线记录 | 每个状态变更精确时间戳结构化存储 |
| 17 | 🔴 | PC/手机端操作按钮统一 | 同一状态下两端按钮一致 |

## 五、通知中心

| 编号 | 状态 | 任务 |
|------|------|------|
| 18 | � | PC + 手机端统一通知中心（小喇叭 + 红点徽章 + 通知列表 + 点击穿透） |
| 19 | � | 通知结构预留微信公众号推送字段 |

> **✅ 修改逻辑**：
> - 新建 `src/stores/notificationStore.js`：统一通知数据，含 `id / type / title / content / targetRole / jumpPath / jumpParams / isRead / wechatPushStatus / createdAt` 字段，`addNotification`、`markAsRead`、`markAllAsRead`、`getNotificationsByRole` 等方法
> - 新建 `src/components/NotificationPanel.vue`：通知面板组件（底部抽屉），按时间倒序展示，未读蓝色高亮，点击穿透跳转，支持全部已读和清空
> - `App.vue` header 右上角增加小喇叭图标 + 红点未读徽章（PC端和手机端均显示），挂载 `NotificationPanel`
> - 已接入通知触发点：
>   - `workorderFlowStore.js`：工单创建（通知课长）、工单分配（通知工程师+客户）
>   - `StaffFieldCheckinList.vue`：打卡提交审批（通知课长）、打卡审批通过（通知工程师）
> - 数据结构预留 `wechatPushStatus` 字段（pending/pushed/failed），后期对接微信无需改数据层
> - 后续通知节点扩展（工单流转/报价审核/超时报警等）仅需在对应 Store 中调用 `addNotification()` 即可

## 六、其他调整

| 编号 | 状态 | 任务 | 说明 |
|------|------|------|------|
| 20 | 🟢 | 客户设备列表隐藏新增按钮 | CustomerDeviceList 客户视图 |
| 21 | 🟢 | 客户不填装机报告 | AssetDetail 客户角色隐藏装机报告按钮 |

> **✅ 编号20 修改逻辑**：`CustomerDeviceList.vue` 删除空状态下的"扫码添加设备"按钮、右下角 FAB "+"悬浮按钮、未使用的 `goToScan` 函数和 `Plus` 图标引入。客户设备由后台管理维护。
>
> **✅ 编号21 修改逻辑**：`AssetDetail.vue` 新增 `isCustomer` 计算属性（检测 `localStorage.customerAuth` 判断是否客户登录），装机报告/装机记录区域改为 `v-if="!isCustomer"`，客户角色不展示该区域。装机报告由工程师填写。

| 22 | 🟢 | 工单分配后隐藏编辑/删除 | 状态 ≥ 已分配后不展示。PC端+手机端同步 |

> **✅ 修改逻辑**：
> - **PC端** `Workorder.vue`：编辑/删除按钮加 `v-if="row.status === '待派单'"`，非待派单状态下仅显示"查看"
> - **手机端** `StaffWorkorderList.vue`：卡片操作区新增编辑/删除按钮，`v-if="order.status === 'pending' && isManager"`（课长/管理员可见，工程师不显示）；新增 `isManager` 计算属性判断当前角色，`handleEditWorkorder` 跳转创建编辑页，`handleDeleteWorkorder` 确认框后删除并同步 localStorage；`getStatusType/getStatusText` 补充 `pending`/`待分配` 映射
> - 两端按钮差异：PC端的编辑/删除不限制角色（三个员工角色均可见），手机端仅课长和管理员可见

| 23 | 🔴 | 数据看板重做 | 改为山善关注的 KPI（待确认指标） |

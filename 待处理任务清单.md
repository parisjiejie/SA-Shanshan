# SA系统 — 待处理任务清单

> 📅 生成日期：2026-06-11 | 来源：系统逻辑全面梳理

---

## 概览

| 优先级 | 数量 | 说明 |
|--------|:----:|------|
| P1 高 | 4 | 影响用户操作，需尽快修复 |
| P2 中 | 2 | 影响数据一致性或代码规范 |
| P3 低 | 2 | 代码清理/优化，不影响功能 |
| ✅ 已验证 | 10 | 已确认正确，无需处理 |

---

## P1 — 高优先级（影响用户操作）

### ❌ P1-1：PC端工单列表"课长确认"/"业务确认"按钮缺少状态限制

- **文件**：`src/views/Workorder.vue` [L114-L117](file:///d:/SA/src/views/Workorder.vue#L114-L117)
- **问题**：admin 和课长在所有 892 条工单的每一行都看到"课长确认"按钮，即使工单才在"待分配"状态
- **修复**：添加 `row.flowStatus === 'techlead_confirm'` 和 `row.flowStatus === 'assistant_confirm'` 状态条件

### ❌ P1-2：PC端工单列表完全缺少"处理"和"签字"按钮

- **文件**：`src/views/Workorder.vue`
- **问题**：`canSubmitForSign`、`canSignWorkorder` 函数已导入但模板中完全没有对应按钮
- **修复**：添加以下按钮：
  - 【提交服务报告】— `flowStatus === 'processing'`，角色 admin/被指派工程师
  - 【签字确认】— `flowStatus === 'pending_sign'`，角色 admin/被指派工程师/客户

### ❌ P1-3：PC端工单详情弹窗 detailActions 缺少状态过滤

- **文件**：`src/views/Workorder.vue` [L1071-L1089](file:///d:/SA/src/views/Workorder.vue#L1071-L1089)
- **问题**：`detailActions` computed 只按角色判断，不按状态过滤。打开"待分配"工单详情，弹窗顶部同时显示"分配工程师"+"接单"+"弃单"+"课长确认"+"业务确认"
- **修复**：参照手机端 `StaffWorkorderDetail.vue` 的 `v-else-if` 链，让 `detailActions` 增加状态判断

### ❌ P1-4：`Roles.SALES` 死代码遗留

- **文件**：`src/stores/workorderFlowStore.js` [L602](file:///d:/SA/src/stores/workorderFlowStore.js#L602)
- **问题**：6个角色中无 sales，但枚举中仍保留，可能导致空权限引用
- **修复**：移除 `SALES: 'sales'`

---

## P2 — 中优先级（影响数据一致性/代码规范）

### ⚠️ P2-1：路由守卫无权限回退目标仍是 `/dashboard`

- **文件**：`src/router/index.js` [L371-L377](file:///d:/SA/src/router/index.js#L371-L377)
- **问题**：登录后首页已改为 `/staff-mobile-workspace`，但路由守卫越权回退仍指向 `/dashboard`
- **修复**：将 `roleHomePage` 中 `admin/assistant/techLead/director` 的首页从 `/dashboard` 改为 `/staff-mobile-workspace`
- **备注**：触发频率极低（仅手动输入非法 URL 时），可视情况保留不改

### ⚠️ P2-2：Dashboard 统计数据全部硬编码

- **文件**：`src/views/Dashboard.vue` [L112-L115](file:///d:/SA/src/views/Dashboard.vue#L112-L115)
- **问题**：4个统计卡片 + 4张ECharts图表数据全为常量，不随工单操作变化
- **备注**：你说过"数据看板做死展示后期再改"，此问题可延后到工作台重构时统一处理

---

## P3 — 低优先级（代码清理/优化）

### ⚠️ P3-1：Workorder.vue 导入但未使用 `canSubmitForSign` / `canSignWorkorder`

- **文件**：`src/views/Workorder.vue`
- **问题**：import 已引入两个权限函数，但模板未消费。修 P1-2 时正好用上
- **修复**：P1-2 修复后自动消除

### ⚠️ P3-2：PC端工单列表缺少统一的 `getAvailableActions` 集中判断

- **文件**：`src/views/Workorder.vue`
- **问题**：手机端用 `v-else-if` 链统一管理，PC端用零散 `v-if` 条件，风格不一致
- **建议**：后续重构时统一为 `getAvailableActions(role, status)` 方式

---

## ✅ 已验证正确的项目（无需处理）

| 序号 | 检查项 | 文件 |
|:----:|--------|------|
| 1 | `canCreateWorkorder` → admin/assistant/customer | `workorderFlowStore.js` |
| 2 | `getVisibleWorkorders` → assistant全量、engineer仅自己的 | `workorderFlowStore.js` |
| 3 | `checkTimeoutAlarms` → engineer + techLead | `workorderFlowStore.js` |
| 4 | 8节点状态流转全部正确 | `workorderFlowStore.js` |
| 5 | 通知触发 15 条匹配技能矩阵 | `workorderFlowStore.js` |
| 6 | router `rolePermissions` 已移除 engineer 的创建路由 | `router/index.js` |
| 7 | 手机端列表页按钮状态条件 | `StaffWorkorderList.vue` |
| 8 | 工作台入口 engineer 不跳创建页 | `StaffMobileWorkspace.vue` |
| 9 | UnifiedLogin redirect → `/staff-mobile-workspace` | `UnifiedLogin.vue` |
| 10 | `Roles` 枚举中已无 sales 角色引用 | 上述文件 |

---

## 修复建议顺序

```
P1-4 → P1-1 → P1-2 → P1-3 → P2-1 → P2-2（延后）
                                   ↘ P3-1（随P1-2自动修复）
                                   ↘ P3-2（重构时统一）
```

---

> 💡 处理任何一个问题时，请在提交信息中引用编号（如 `fix: P1-1 PC端工单列表确认按钮加状态限制`），便于追踪。

---

## 2026-04-24

### 优化：设备详情页（扫码查询）电脑端居中显示

#### 修改文件：`src/views/AssetDetail.vue`

**修改内容：**
- 优化 `@media (min-width: 768px)` 响应式样式
- `.asset-detail` 容器添加 flex 布局，实现水平和垂直居中
- `.mobile-asset-detail` 添加以下样式：
  - `width: 100%` - 确保宽度自适应
  - `min-height: calc(100vh - 40px)` - 减去padding后的最小高度
  - `border-radius: 12px` - 圆角边框
  - `box-shadow: 0 4px 20px rgba(0,0,0,0.08)` - 阴影效果
- 背景色从白色改为浅灰色 `#f0f2f5`，手机端内容区域保持白色背景

**效果：**
- 电脑端访问时，手机端页面会显示在画面正中央
- 添加了圆角和阴影，模拟手机设备外观
- 四周留有灰色边距，视觉层次更清晰

---

## 2026-04-24

### 新增：报价单列表追加客户已确认数据（用于演示转订单功能）

#### 修改文件：`src/stores/quotationStore.js`

**新增内容：**
- 在 `initMockData()` 函数中的 `mockQuotations` 数组追加一条客户已确认状态的报价单数据
- 报价单编号：`QT20260424001`
- 关联工单：`WO20260424001`
- 客户信息：某某设备有限公司 / 陈经理
- 包含3项配件：
  - 主轴轴承 ZB-150 × 2套
  - 电机驱动器 JD-80 × 1台
  - 导轨滑块 DL-25 × 4套
- 状态：`confirmed`（客户已确认）
- 包含完整的审批记录、发送记录、客户签字确认信息

**用途：**
- 用于演示报价单转订单功能
- 在报价单列表页面筛选"已确认"状态可查看
- 点击查看详情后可操作"转工单"按钮

---

## 2026-04-09

### 前端开发：整合维修工单报价单功能（方案二）

#### 1. 修改文件：`src/stores/quotationStore.js`

**新增内容：**
- 添加 `QuotationType` 枚举（配件销售/维修工单）
- 分离存储：`quotations`（配件销售）和 `workorderQuotations`（维修工单）
- 新增计算属性：`workorderQuotationList`、`workorderPendingApprovalCount`、`totalPendingApprovalCount`
- 新增方法：
  - `createWorkorderQuotation()` - 创建维修工单报价单
  - `submitWorkorderQuotationForApproval()` - 提交维修工单报价单审核
  - `approveWorkorderQuotation()` - 审批维修工单报价单
  - `sendWorkorderQuotation()` - 发送维修工单报价单给客户
  - `confirmWorkorderQuotation()` - 客户确认维修工单报价单
  - `getWorkorderQuotationsByWorkorder()` - 根据工单ID获取维修工单报价单
  - `getPendingWorkorderQuotations()` - 获取所有待审核的维修工单报价单

**存储方案：**
- 配件销售报价单：`localStorage` - `quotations` key
- 维修工单报价单：`localStorage` - `workorderQuotations` key
- 统一审批记录：`state.approvalRecords`

**兼容性处理：**
- 加载旧数据时自动添加 `type: QuotationType.PARTS_SALE` 字段

#### 2. 修改文件：`src/views/Workorder.vue`

**修改内容：**
- 更新导入：使用维修工单专用的 quotationStore 方法
- `workorderQuotations` 改为 computed 属性，自动从 `quotationStore` 获取
- `submitQuotationPDF()` 使用 `createWorkorderQuotation()` 创建报价单
- `handleSubmitQuotationForApproval()` 使用 `submitWorkorderQuotationForApproval()`
- `approveQuotationLocal()` 使用 `approveWorkorderQuotation()`
- `rejectQuotationLocal()` 使用 `approveWorkorderQuotation()`

#### 3. 修改文件：`src/views/StaffMobileWorkspace.vue`

**修改内容：**
- 更新导入：使用 `getPendingWorkorderQuotations` 方法
- `pendingQuotations` computed 属性使用 `quotationStore` 方法获取待审核报价单

#### 4. 数据结构设计

**配件销售报价单：**
```javascript
{
  id: "QT123456",
  type: "parts_sale",
  items: [...],           // 配件明细
  subtotal: 200,          // 自动计算
  taxAmount: 26,          // 自动计算
  totalAmount: 226,       // 自动计算
  pdfUrl: null,           // 系统生成
  // ...
}
```

**维修工单报价单：**
```javascript
{
  id: "QT789012",
  type: "workorder_repair",
  workorderId: "WO001",   // 关联工单
  pdfUrl: "blob:...",     // 用户上传
  fileName: "报价单.pdf",
  totalAmount: 0,         // 手动填写
  items: [],              // 空数组
  // ...
}
```

#### 5. 审批流程统一

两种报价单使用统一的审批状态：
- `draft` → `pending` → `approved/rejected` → `sent` → `confirmed`

技术部长可以在工作台查看所有待审核的维修工单报价单，统一处理。

#### 6. 对现有功能的影响

**配件销售报价单（Quotation.vue）：**
- ✅ 无影响，功能完全不变
- ✅ 只需添加 `type` 字段标记
- ✅ 数据存储位置不变

**维修工单报价单（Workorder.vue）：**
- ✅ 使用独立的存储和方法
- ✅ 统一的审批流程
- ✅ 便于后续扩展

---

## 2026-04-03

### 后端开发：完成核心业务模块Service和Controller

#### 1. 设备管理模块 (Asset)

**新增文件：**
- `backend/src/main/java/com/aftersales/dto/AssetDTO.java` - 设备数据传输对象
- `backend/src/main/java/com/aftersales/mapper/AssetMapper.java` - 设备对象映射器
- `backend/src/main/java/com/aftersales/service/AssetService.java` - 设备服务层
- `backend/src/main/java/com/aftersales/controller/AssetController.java` - 设备控制器

**功能特性：**
- 设备CRUD操作
- 根据序列号查询设备
- 获取客户设备列表
- 设备统计（运行中、维修中、停机、报废）
- 批量删除设备

**API接口：**
- GET /assets - 获取设备列表
- GET /assets/{id} - 获取设备详情
- GET /assets/serial/{serialNumber} - 根据序列号获取设备
- POST /assets - 创建设备
- PUT /assets/{id} - 更新设备
- DELETE /assets/{id} - 删除设备
- GET /assets/statistics - 获取设备统计

#### 2. 工单管理模块 (Workorder)

**新增文件：**
- `backend/src/main/java/com/aftersales/dto/WorkorderDTO.java` - 工单数据传输对象
- `backend/src/main/java/com/aftersales/mapper/WorkorderMapper.java` - 工单对象映射器
- `backend/src/main/java/com/aftersales/service/WorkorderService.java` - 工单服务层
- `backend/src/main/java/com/aftersales/controller/WorkorderController.java` - 工单控制器

**功能特性：**
- 工单CRUD操作
- 工单状态流转（待处理→已接单→处理中→已完成）
- 接单、开始处理、完成、取消操作
- 工单号自动生成（WO+日期+序号）
- 关联客户、设备、工程师信息

**API接口：**
- GET /workorders - 获取工单列表
- POST /workorders - 创建工单
- POST /workorders/{id}/accept - 接单
- POST /workorders/{id}/start - 开始处理
- POST /workorders/{id}/complete - 完成工单
- POST /workorders/{id}/cancel - 取消工单

#### 3. 配件管理模块 (Part)

**新增文件：**
- `backend/src/main/java/com/aftersales/dto/PartDTO.java` - 配件数据传输对象
- `backend/src/main/java/com/aftersales/mapper/PartMapper.java` - 配件对象映射器
- `backend/src/main/java/com/aftersales/service/PartService.java` - 配件服务层
- `backend/src/main/java/com/aftersales/controller/PartController.java` - 配件控制器

**功能特性：**
- 配件CRUD操作
- 库存管理（充足、紧张、缺货自动计算）
- 库存调整功能
- 低库存配件查询
- 配件统计（库存总值计算）

**API接口：**
- GET /parts - 获取配件列表
- POST /parts - 创建配件
- POST /parts/{id}/stock/adjust - 调整库存
- GET /parts/low-stock - 获取低库存配件
- GET /parts/statistics - 获取配件统计

#### 4. 报价单管理模块 (Quotation)

**新增文件：**
- `backend/src/main/java/com/aftersales/dto/QuotationDTO.java` - 报价单数据传输对象
- `backend/src/main/java/com/aftersales/dto/QuotationItemDTO.java` - 报价单项数据传输对象
- `backend/src/main/java/com/aftersales/mapper/QuotationMapper.java` - 报价单对象映射器
- `backend/src/main/java/com/aftersales/service/QuotationService.java` - 报价单服务层
- `backend/src/main/java/com/aftersales/controller/QuotationController.java` - 报价单控制器

**功能特性：**
- 报价单CRUD操作
- 报价单状态流转（草稿→待审核→已审核→已发送→客户确认/拒绝）
- 报价单号自动生成（QT+日期+序号）
- 总金额自动计算
- 有效期管理

**API接口：**
- GET /quotations - 获取报价单列表
- POST /quotations - 创建报价单
- POST /quotations/{id}/submit - 提交审核
- POST /quotations/{id}/approve - 审核报价单
- POST /quotations/{id}/send - 发送报价单
- POST /quotations/{id}/confirm - 客户确认
- POST /quotations/{id}/reject - 客户拒绝

#### 5. 联系人管理模块 (Contact)

**新增文件：**
- `backend/src/main/java/com/aftersales/dto/ContactDTO.java` - 联系人数据传输对象
- `backend/src/main/java/com/aftersales/mapper/ContactMapper.java` - 联系人对象映射器
- `backend/src/main/java/com/aftersales/service/ContactService.java` - 联系人服务层
- `backend/src/main/java/com/aftersales/controller/ContactController.java` - 联系人控制器

**功能特性：**
- 联系人CRUD操作
- 审核状态管理（待审核、已通过、已拒绝）
- 手机号唯一性校验
- 获取客户已审核联系人
- 联系人统计

**API接口：**
- GET /contacts - 获取联系人列表
- POST /contacts - 创建联系人
- POST /contacts/{id}/approve - 审核联系人
- GET /contacts/customer/{customerId}/approved - 获取客户已审核联系人
- GET /contacts/statistics - 获取联系人统计

#### 后端开发完成度更新

| 层级 | 完成度 | 状态 |
|------|--------|------|
| Entity | 100% | ✅ |
| Repository | 100% | ✅ |
| Service | 100% | ✅ |
| Controller | 100% | ✅ |
| DTO | 100% | ✅ |
| Mapper | 100% | ✅ |

**总体完成度: 约 95%**

---

## 2026-04-03

### 配置更新：天眼查API已启用

#### 配置内容
- **API Key**: 已配置（存储在 .env 文件）
- **状态**: 已启用 (enabled: true)
- **配置文件**: 
  - `backend/.env` - 环境变量文件
  - `backend/src/main/resources/application.yml` - 应用配置

#### 测试接口
启动后端服务后，可通过以下接口测试：

```bash
# 1. 搜索企业
POST http://localhost:8080/api/third-party/tianyancha/search
Content-Type: application/json

{
  "keyword": "阿里巴巴",
  "pageNum": 1,
  "pageSize": 10
}

# 2. 获取企业详情
POST http://localhost:8080/api/third-party/tianyancha/detail
Content-Type: application/json

{
  "name": "阿里巴巴（中国）有限公司"
}

# 3. 根据名称获取详情（简化接口）
GET http://localhost:8080/api/third-party/tianyancha/detail-by-name?companyName=阿里巴巴

# 4. 获取风险信息
GET http://localhost:8080/api/third-party/tianyancha/risk/{companyId}
```

#### 前端测试页面
- **路径**: `/tianyancha-test`
- **功能**: 
  - 企业搜索测试
  - 企业详情查询测试
  - 实时显示API返回结果
  - JSON格式化展示

#### 测试步骤
1. 启动后端服务（端口8080）
2. 访问前端页面：`http://localhost:3002/tianyancha-test`
3. 在测试页面输入企业名称
4. 点击"搜索"或"查询详情"按钮
5. 查看返回的JSON结果

#### 注意事项
- `.env` 文件包含敏感信息，已自动添加到 `.gitignore`（如未添加请手动添加）
- 重启后端服务后配置生效
- API调用日志会自动记录到 `api_call_log` 表
- 天眼查API有调用频率限制，请注意使用频率

---

## 2026-04-03

### 新增：第三方API集成 - 天眼查、高德地图、短信服务

#### 1. 配置文件

**application.yml 更新**
- 添加 `third-party` 配置节点
- 天眼查配置：API Key、Secret、超时设置
- 高德地图配置：Web服务Key、安全密钥、超时设置
- 短信服务配置：服务商选择、验证码设置、防刷配置
- 支持阿里云和腾讯云短信服务商

**环境变量配置 (.env.example)**
- 提供完整的环境变量示例文件
- 包含所有第三方API的配置项
- 标注各API的申请地址

#### 2. API服务层

**配置类 (ThirdPartyApiConfig.java)**
- 统一的第三方API配置管理
- 支持天眼查、高德地图、短信服务配置
- 内嵌配置类：TianyanchaConfig、AmapConfig、SmsConfig

**DTO类**
- TianyanchaDTO：企业搜索、详情、股东、风险信息
- AmapDTO：地理编码、逆地理编码、路径规划、地点搜索
- SmsDTO：发送短信、验证码、验证请求/响应
- ApiCallLogDTO：API调用日志

**实体类**
- ApiCallLog：API调用日志实体
- SmsSendRecord：短信发送记录实体

**Mapper接口**
- ApiCallLogMapper：API调用日志数据访问
- SmsSendRecordMapper：短信发送记录数据访问

#### 3. Service服务层

**ApiCallLogService**
- 记录API调用日志
- 支持请求参数、响应数据、耗时、状态记录

**TianyanchaService / TianyanchaServiceImpl**
- searchCompany：企业搜索
- getCompanyDetail：获取企业详情
- getCompanyDetailByName：根据名称获取详情
- getCompanyRiskInfo：获取风险信息
- 支持模拟数据模式（服务未启用时）

**AmapService / AmapServiceImpl**
- geocode：地理编码（地址转坐标）
- regeocode：逆地理编码（坐标转地址）
- direction：路径规划
- placeSearch：地点搜索
- getLocationByAddress：简化接口-地址转坐标
- getAddressByLocation：简化接口-坐标转地址
- calculateRoute：简化接口-计算路线
- 支持模拟数据模式（服务未启用时）

**SmsService / SmsServiceImpl**
- sendSms：发送短信
- sendVerifyCode：发送验证码（带防刷）
- verifyCode：验证验证码
- getStatistics：获取发送统计
- canSend：检查是否可以发送
- 支持阿里云短信SDK
- 支持模拟发送模式（未配置模板时）

#### 4. Controller控制器

**ThirdPartyApiController**
- 提供RESTful API接口
- 天眼查接口：/third-party/tianyancha/**
- 高德地图接口：/third-party/amap/**
- 短信接口：/third-party/sms/**
- 统一的返回格式封装

#### 5. 数据库表

**api_call_log 表**
- 记录所有第三方API调用
- 字段：api_type、api_name、request_params、response_data、status、error_msg、cost_time
- 索引：api_type、api_name、status、create_time

**sms_send_record 表**
- 记录短信发送历史
- 字段：phone、template_code、verify_code、status、send_time、expire_time、verify_time
- 索引：phone、verify_code、status、create_time

#### 6. Maven依赖

**pom.xml 新增**
- 阿里云短信SDK：com.aliyun:dysmsapi20170525:3.0.0
- Hutool工具包：cn.hutool:hutool-all:5.8.23
- MyBatis Plus：com.baomidou:mybatis-plus-boot-starter:3.5.5

#### 7. API接口清单

| 接口 | 方法 | 说明 |
|------|------|------|
| /third-party/tianyancha/search | POST | 搜索企业 |
| /third-party/tianyancha/detail | POST | 获取企业详情 |
| /third-party/tianyancha/detail-by-name | GET | 根据名称获取详情 |
| /third-party/tianyancha/risk/{companyId} | GET | 获取风险信息 |
| /third-party/amap/geocode | POST | 地理编码 |
| /third-party/amap/regeocode | POST | 逆地理编码 |
| /third-party/amap/direction | POST | 路径规划 |
| /third-party/amap/place-search | POST | 地点搜索 |
| /third-party/amap/location | GET | 地址转坐标（简化） |
| /third-party/amap/address | GET | 坐标转地址（简化） |
| /third-party/amap/route | GET | 计算路线（简化） |
| /third-party/sms/verify-code | POST | 发送验证码 |
| /third-party/sms/verify-code/validate | POST | 验证验证码 |
| /third-party/sms/can-send | GET | 检查是否可发送 |
| /third-party/sms/statistics | GET | 发送统计 |

#### 8. 使用说明

**启用服务**
1. 申请各平台API账号
2. 复制 `.env.example` 为 `.env`
3. 填写实际的API密钥
4. 修改 `application.yml` 中对应服务的 `enabled: true`

**短信验证码流程**
```
1. 调用 POST /third-party/sms/verify-code 发送验证码
2. 用户输入验证码
3. 调用 POST /third-party/sms/verify-code/validate 验证
```

**企业信息查询流程**
```
1. 调用 POST /third-party/tianyancha/search 搜索企业
2. 调用 POST /third-party/tianyancha/detail 获取详情
```

**地址解析流程**
```
1. 调用 GET /third-party/amap/location?address=xxx 获取坐标
2. 调用 GET /third-party/amap/address?longitude=xxx&latitude=xxx 获取地址
```

#### 9. 文件清单

**新增文件：**
- config/ThirdPartyApiConfig.java
- dto/TianyanchaDTO.java
- dto/AmapDTO.java
- dto/SmsDTO.java
- dto/ApiCallLogDTO.java
- entity/ApiCallLog.java
- entity/SmsSendRecord.java
- mapper/ApiCallLogMapper.java
- mapper/SmsSendRecordMapper.java
- service/ApiCallLogService.java
- service/TianyanchaService.java
- service/AmapService.java
- service/SmsService.java
- service/impl/ApiCallLogServiceImpl.java
- service/impl/TianyanchaServiceImpl.java
- service/impl/AmapServiceImpl.java
- service/impl/SmsServiceImpl.java
- controller/ThirdPartyApiController.java
- .env.example
- sql/api_tables.sql

**修改文件：**
- pom.xml（添加依赖）
- application.yml（添加配置）

---

## 2026-04-02

### Bug修复：字段配置系统功能验证与修复

#### 修复问题清单

**1. DynamicFormV2.vue 组件不完整**
- **问题**: 组件在第273行被截断，缺少完整的联动事件处理
- **修复**: 补充完整的联动规则处理逻辑
  - 添加 `handleLinkageValueChange` 处理值变化
  - 添加 `handleLinkageVisibility` 处理可见性
  - 添加 `handleLinkageDisabled` 处理禁用状态
  - 添加 `handleLinkageOptions` 处理选项变化
  - 添加组件卸载时的清理逻辑

**2. 联动规则引擎事件监听**
- **问题**: 联动引擎事件监听不完整
- **修复**: 完善事件监听和取消监听机制
  - 监听 `valueChange` 事件
  - 监听 `fieldVisibility` 事件
  - 监听 `fieldDisabled` 事件
  - 监听 `fieldOptions` 事件

**3. 表单字段渲染器模式传递**
- **问题**: FormFieldRenderer 组件缺少 mode 属性
- **修复**: 添加 `:mode="readonly ? 'preview' : 'design'"` 属性

**4. 组件样式完善**
- **问题**: 缺少字段隐藏和禁用的样式
- **修复**: 添加 `.is-hidden` 和 `.is-disabled` 样式类

**5. FieldConfigManager.vue 表格数据类型错误**
- **问题**: `filteredFields` 计算属性返回的数据不是数组，导致 Element Plus 表格报错 `data.filter is not a function`
- **修复**: 
  - 在 `filteredFields` 计算属性中添加数组类型检查
  - 确保 `fieldConfigStore.allFields` 始终返回数组
  - 添加默认值 `[]` 防止 undefined

**6. fieldConfigStore.js allFields 计算属性**
- **问题**: `allFields` 计算属性直接返回 `fieldDefinitions.value`，可能不是数组
- **修复**: 修改计算属性，确保始终返回数组类型
  ```javascript
  const allFields = computed(() => {
    return Array.isArray(fieldDefinitions.value) ? fieldDefinitions.value : []
  })
  ```

**7. 多个页面中 `operation-width` 属性绑定错误**
- **问题**: `:operation-width="auto"` 使用了动态绑定，但 `auto` 不是变量，导致 Vue 警告 `Property "auto" was accessed during render but is not defined on instance`
- **影响页面**:
  - Asset.vue
  - Workorder.vue
  - Quotation.vue
  - Customer.vue
  - Parts.vue
- **修复**: 将 `:operation-width="auto"` 改为 `operation-width="auto"`（去掉冒号，使用静态字符串）

**8. FieldTemplateManager.vue 模板数据 undefined 错误**
- **问题**: `Cannot read properties of undefined (reading 'id')`，`filteredTemplates` 计算属性返回的数据中包含 undefined 或 null 项
- **修复**:
  - 在 `fieldTemplateStore.js` 中修改 `allTemplates` 计算属性，确保始终返回数组
  - 在 `FieldTemplateManager.vue` 中修改 `filteredTemplates` 计算属性，添加数组类型检查
  - 添加可选链操作符 `?.` 防止访问 undefined 属性
  - 过滤掉无效的模板数据（`templates.filter(t => t && t.id)`）

#### 验证结果

✅ **fieldTypeRegistry.js** - 正常，26种字段类型定义完整  
✅ **fieldConfigStore.js** - 正常，字段配置管理功能完整  
✅ **fieldTemplateStore.js** - 正常，模板管理功能完整  
✅ **DynamicFormV2.vue** - 已修复，组件功能完整  
✅ **linkageEngine.js** - 正常，联动规则引擎完整  
✅ **fieldConfigCache.js** - 正常，缓存机制完整  
✅ **formLayoutStore.js** - 正常，布局模板完整  
✅ **路由配置** - 正常，所有页面路由已配置  
✅ **菜单配置** - 正常，管理员菜单已添加  

#### 系统功能状态

| 功能模块 | 状态 | 说明 |
|---------|------|------|
| 字段类型管理 | ✅ 正常 | 26种字段类型可用 |
| 字段配置管理 | ✅ 正常 | CRUD功能完整 |
| 字段设计器 | ✅ 正常 | 拖拽设计可用 |
| 模板管理 | ✅ 正常 | 模板CRUD完整 |
| 客户定制 | ✅ 正常 | 差异对比可用 |
| 联动规则 | ✅ 正常 | 规则引擎完整 |
| DynamicFormV2 | ✅ 已修复 | 表单渲染正常 |
| 缓存机制 | ✅ 正常 | localStorage缓存可用 |
| 布局模板 | ✅ 正常 | 5种预设布局可用 |

---

## 2026-04-02

### 新增：系统集成与优化（任务1-6）

#### 1. 系统集成：DynamicFormV2 组件

**文件**: `src/components/DynamicFormV2.vue`

**功能**:
- 支持字段模板渲染（通过 templateId）
- 支持客户定制配置（通过 customerId）
- 支持流程节点字段过滤（通过 nodeCode）
- 支持角色权限控制（通过 userRole）
- 24列栅格布局系统
- 动态表单验证规则
- 字段可见性控制
- 字段禁用状态控制
- 联动规则集成

**使用示例**:
```vue
<DynamicFormV2
  templateId="template_repair_standard"
  :customerId="customerId"
  nodeCode="WORKING"
  v-model="formData"
/>
```

#### 2. 性能优化：字段配置缓存机制